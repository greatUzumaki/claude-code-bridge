# Sample Project

This is a fixture project used by Playwright end-to-end tests.

It exists so that:

- The sidebar has at least one discoverable project (`sample-project`)
- The file tree has at least one file (`README.md`)
